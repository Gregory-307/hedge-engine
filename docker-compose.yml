version: "3.9"

services:
  api:
    build: .
    container_name: hedge-api
    ports:
      - "8000:8000"
    environment:
      LOG_LEVEL: INFO
    # The API is stateless - no database or cache required.
    # All risk calculations are done in-memory per request.
